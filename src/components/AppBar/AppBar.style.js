import { createUseStyles } from 'react-jss';

export default createUseStyles({
  root: {
    backgroundColor: '#000000',
    display: 'flex',
    width: '100%',
    height: '80px',
  },
}, { name: 'AppBar' });